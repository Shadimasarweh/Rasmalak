'use client';

/**
 * usePermission — check if current user has a specific CRM permission.
 * Re-exported from orgStore for convenience.
 */
export { useOrgPermission as usePermission } from '@/store/orgStore';
