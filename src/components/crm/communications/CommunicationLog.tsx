'use client';

/**
 * CommunicationLog — re-exports ContactTimeline for use in standalone contexts.
 * The timeline component already renders communications chronologically.
 */
export { ContactTimeline as CommunicationLog } from '@/components/crm/contacts/ContactTimeline';
